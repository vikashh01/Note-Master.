package FullProject;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;
import java.time.Duration;

public class SignupTest {
    WebDriver driver;
    WebDriverWait wait;  // Declare once and use globally

    @BeforeMethod(alwaysRun = true)
    public void setup() {
        driver = new ChromeDriver();
        driver.manage().window().maximize();
        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10)); // Set implicit wait
        wait = new WebDriverWait(driver, Duration.ofSeconds(10)); // Set explicit wait globally
        driver.get("https://notes-makers.vercel.app/");
    }

    @Test(priority = 1)
    public void testSignup() {
        driver.findElement(By.cssSelector("button:nth-child(2) strong")).click();
        driver.findElement(By.id("name")).sendKeys("Akash");
        driver.findElement(By.id("email")).sendKeys("akash123@gmail.com");
        driver.findElement(By.id("password")).sendKeys("akash@123");
        driver.findElement(By.id("confirmPassword")).sendKeys("Akash@123");
        driver.findElement(By.xpath("//button/strong[contains(text(),'Sign Up')]")).click();
    }

    @Test(priority = 2)
    public void testLogin() {
        driver.findElement(By.cssSelector("nav button:nth-child(1) strong:nth-child(1)")).click();
        driver.findElement(By.id("email")).sendKeys("akash123@gmail.com");
        driver.findElement(By.id("password")).sendKeys("akash@123");
        driver.findElement(By.xpath("//button/strong[contains(text(),'Login')]")).click();
    }

    @Test(priority = 3)
    public void testCreateNote() {
        driver.findElement(By.xpath("//strong[normalize-space()='Write Notes']")).click();
        driver.findElement(By.cssSelector("input[placeholder='Notes Title']")).sendKeys("First Note");
        WebElement editor = driver.findElement(By.cssSelector(".ql-editor"));
        editor.click();
        editor.sendKeys("This is my first note.");
        driver.findElement(By.xpath("//strong[normalize-space()='Save Changes']")).click();
    }

    @Test(priority = 4)
    public void createNotebook() {
        driver.findElement(By.xpath("//strong[normalize-space()='Create NoteBook']")).click();
        driver.findElement(By.id("title")).sendKeys("This is a new notebook.");
        driver.findElement(By.xpath("//strong[normalize-space()='Submit']")).click();
    }

    @Test(priority = 5)
    public void editNote() {
        driver.findElement(By.cssSelector("main div div button:nth-child(1) strong:nth-child(1)")).click();
        WebElement editor = driver.findElement(By.cssSelector(".ql-editor"));
        editor.click();
        editor.sendKeys("New update");
        driver.findElement(By.xpath("//strong[normalize-space()='Save Changes']")).click();
    }

    @AfterMethod(alwaysRun = true)
    public void tearDown() {
        driver.quit();
    }
}
